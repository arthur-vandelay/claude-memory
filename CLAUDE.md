These are persistent memory files for Claude Code. Read all .md files under projects/ and treat their contents as your memory/context for this conversation. Follow any preferences and instructions found in the MEMORY.md files.

IMPORTANT — READ-ONLY ON MOBILE:
If you are running on mobile (Claude app, not a desktop machine), do NOT modify, overwrite, or push changes to any memory files in this repo. Memory files are managed from the desktop machines only. On mobile, you may READ memory for context but never WRITE to it. If the user wants to save a new memory on mobile, tell them it will be saved next time they're on desktop, or ask them to note it down.
